{
  "id": "0074-inspect-core-span-objects",
  "type": "execute_code",
  "params": {
    "code": "import bpy, os\nfrom mathutils import Vector\nexpected=os.path.normcase(os.path.normpath(r'C:\\mcp-blender\\salvador_lacerda_do_zero.blend'))\nif os.path.normcase(os.path.normpath(bpy.data.filepath))!=expected: raise RuntimeError('arquivo errado')\nfor n in ('Ladeira da Montanha.003','OSM 59409445.001','OSM 574226774.001'):\n o=bpy.data.objects.get(n); print('---',n,'---')\n if not o: print('MISSING'); continue\n ps=[o.matrix_world@Vector(c) for c in o.bound_box]; dims=Vector((max(p.x for p in ps)-min(p.x for p in ps),max(p.y for p in ps)-min(p.y for p in ps),max(p.z for p in ps)-min(p.z for p in ps)))\n print('TYPE',o.type,'DIMS',tuple(round(x,2) for x in dims),'VERTS',len(o.data.vertices) if o.type=='MESH' else 'NA')\n print('PROPS', {k:o[k] for k in o.keys() if k!='_RNA_UI'})\nprint('CORE_SPAN_INSPECT_OK')"
  },
  "enabled": true
}
