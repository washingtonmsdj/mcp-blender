{
  "id": "0072-inspect-interior-anchors",
  "type": "execute_code",
  "params": {
    "code": "import bpy, os\nfrom mathutils import Vector\nexpected=os.path.normcase(os.path.normpath(r'C:\\mcp-blender\\salvador_lacerda_do_zero.blend'))\nif os.path.normcase(os.path.normpath(bpy.data.filepath))!=expected: raise RuntimeError('arquivo errado')\ne=bpy.data.collections['01 WORLD | ELEVADOR LACERDA | blockout v4']\nkeys=('torre documental','passadico','hall piso terreo','piso nivel 2','piso nivel 3','parede lateral esquerda','parede lateral direita','porta entrada')\nfor k in keys:\n print('---'+k+'---')\n for o in e.all_objects:\n  if k in o.name.lower() and o.type=='MESH':\n   ps=[o.matrix_world@Vector(c) for c in o.bound_box]; mn=Vector((min(p.x for p in ps),min(p.y for p in ps),min(p.z for p in ps))); mx=Vector((max(p.x for p in ps),max(p.y for p in ps),max(p.z for p in ps))); c=(mn+mx)/2\n   print(f'{o.name}|CENTER={tuple(round(v,3) for v in c)}|SIZE={tuple(round(v,3) for v in (mx-mn))}|ROT={tuple(round(v,4) for v in o.rotation_euler)}')\nlow=bpy.data.collections.get('D | ESTACAO CIDADE BAIXA | blockout v1')\nif low:\n ps=[]\n for o in low.objects:\n  if o.type=='MESH': ps += [o.matrix_world@Vector(c) for c in o.bound_box]\n mn=Vector((min(p.x for p in ps),min(p.y for p in ps),min(p.z for p in ps))); mx=Vector((max(p.x for p in ps),max(p.y for p in ps),max(p.z for p in ps))); print('LOW_CENTER='+repr(tuple(round(v,3) for v in ((mn+mx)/2)))+' LOW_SIZE='+repr(tuple(round(v,3) for v in (mx-mn))))\nprint('INTERIOR_ANCHORS_OK')"
  },
  "enabled": true
}
