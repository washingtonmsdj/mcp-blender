{
  "id": "0149-measure-live-bridge-and-upper-building",
  "type": "execute_code",
  "params": {
    "code": "import bpy, os, json, ast, math, urllib.request, xml.etree.ElementTree as ET\nfrom mathutils import Vector, Matrix\nEXPECTED=os.path.normcase(os.path.normpath(r'C:\\mcp-blender\\salvador_lacerda_do_zero.blend'))\nif os.path.normcase(os.path.normpath(bpy.data.filepath))!=EXPECTED: raise RuntimeError(f'ABORTADO arquivo errado {bpy.data.filepath!r}')\norigin=bpy.data.objects.get('WORLD | ORIGEM ELEVADOR LACERDA'); tower=bpy.data.objects.get('V4 | torre documental'); hall=bpy.data.objects.get('V4 | hall piso terreo marcador')\nif not origin or not tower or not hall: raise RuntimeError('anchors ausentes')\nM=ast.literal_eval(str(origin['utm_to_blender_matrix']))\ndef wgs_to_utm24s(lon,lat):\n a=6378137.0; f=1/298.257222101; e2=f*(2-f); ep2=e2/(1-e2); k0=.9996; lon0=math.radians(-39); p=math.radians(lat); l=math.radians(lon); N=a/math.sqrt(1-e2*math.sin(p)**2); T=math.tan(p)**2; C=ep2*math.cos(p)**2; A=math.cos(p)*(l-lon0); e4=e2*e2; e6=e4*e2; mm=a*((1-e2/4-3*e4/64-5*e6/256)*p-(3*e2/8+3*e4/32+45*e6/1024)*math.sin(2*p)+(15*e4/256+45*e6/1024)*math.sin(4*p)-(35*e6/3072)*math.sin(6*p)); E=500000+k0*N*(A+(1-T+C)*A**3/6+(5-18*T+T*T+72*C-58*ep2)*A**5/120); NN=k0*(mm+N*math.tan(p)*(A*A/2+(5-T+9*C+4*C*C)*A**4/24+(61-58*T+T*T+600*C-330*ep2)*A**6/720));\n if lat<0: NN+=1e7\n return E,NN\ndef to_bl(lon,lat):\n e,n=wgs_to_utm24s(lon,lat); return Vector((M[0][0]*e+M[0][1]*n+M[0][2],M[1][0]*e+M[1][1]*n+M[1][2]))\ndef fetch_way(oid):\n req=urllib.request.Request('https://api.openstreetmap.org/api/0.6/way/'+oid+'/full',headers={'User-Agent':'Salvador-Lacerda-QA/1.0'}); data=urllib.request.urlopen(req,timeout=15).read(); root=ET.fromstring(data); nd={n.attrib['id']:(float(n.attrib['lat']),float(n.attrib['lon'])) for n in root.findall('node')}; way=next(w for w in root.findall('way') if w.attrib.get('id')==oid); refs=[x.attrib['ref'] for x in way.findall('nd')]; pts=[to_bl(nd[r][1],nd[r][0]) for r in refs if r in nd]; return refs,pts,{t.attrib['k']:t.attrib['v'] for t in way.findall('tag')}\nT=tower.matrix_world.translation.xy.copy(); H=hall.matrix_world.translation.xy.copy(); EX=(H-T).normalized(); EY=Vector((-EX.y,EX.x))\nrefs_b,bpts,btags=fetch_way('59409445'); refs_s,spts,stags=fetch_way('1263035781')\nbridge_len=(bpts[-1]-bpts[0]).length; bridge_axis=(bpts[-1]-bpts[0]).normalized(); bridge_angle=math.degrees(math.acos(max(-1,min(1,abs(bridge_axis.dot(EX))))))\n# remove closing duplicate for footprint\nif (spts[-1]-spts[0]).length<.01: spts=spts[:-1]\nc=sum((p.x for p in spts),0.0)/len(spts); d=sum((p.y for p in spts),0.0)/len(spts); cen=Vector((c,d)); xx=sum((p.x-c)**2 for p in spts)/len(spts); yy=sum((p.y-d)**2 for p in spts)/len(spts); xy=sum((p.x-c)*(p.y-d) for p in spts)/len(spts); trc=xx+yy; disc=math.sqrt(max(0,(xx-yy)**2+4*xy*xy)); lam=(trc+disc)/2; v=Vector((xy,lam-xx)) if abs(xy)+abs(lam-xx)>1e-12 else Vector((1,0)); v.normalize(); w=Vector((-v.y,v.x)); pv=[(p-cen).dot(v) for p in spts]; pw=[(p-cen).dot(w) for p in spts]; ext_v=max(pv)-min(pv); ext_w=max(pw)-min(pw);\nif ext_w>ext_v: v,w=w,v; ext_v,ext_w=ext_w,ext_v\nangle_ex=math.degrees(math.acos(max(-1,min(1,abs(v.dot(EX)))))); svals=[(p-T).dot(EX) for p in spts]; lvals=[(p-T).dot(EY) for p in spts]; bridge_s=[(p-T).dot(EX) for p in bpts]; bridge_l=[(p-T).dot(EY) for p in bpts]\nres={'bridge_osm':{'tags':btags,'refs':refs_b,'length_m':round(bridge_len,4),'angle_to_v4_axis_deg':round(bridge_angle,4),'s':[round(x,4) for x in bridge_s],'l':[round(x,4) for x in bridge_l]},'upper_building_osm':{'tags':stags,'centroid_xy':[round(c,4),round(d,4)],'centroid_s_l':[round((cen-T).dot(EX),4),round((cen-T).dot(EY),4)],'s_range':[round(min(svals),4),round(max(svals),4)],'l_range':[round(min(lvals),4),round(max(lvals),4)],'pca_extents_m':[round(ext_v,4),round(ext_w,4)],'principal_angle_to_v4_axis_deg':round(angle_ex,4)},'documentary_targets':{'bridge_or_clear_span_m':28.71,'upper_building_m':[18.6,11.0]},'current_v4':{'tower_face_s':1.775,'bridge_s_range':[1.775,30.485],'station_s_range':[30.485,49.085],'station_center_s':39.785}}\nprint('LIVE_BRIDGE_BUILDING_MEASURE_BEGIN'); print(json.dumps(res,ensure_ascii=False,separators=(',',':'))); print('LIVE_BRIDGE_BUILDING_MEASURE_END')"
  },
  "enabled": true
}
