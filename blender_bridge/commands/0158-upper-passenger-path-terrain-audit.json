{
  "id": "0158-upper-passenger-path-terrain-audit",
  "type": "execute_code",
  "params": {
    "code": "import bpy, os, json, ast, math, urllib.request, xml.etree.ElementTree as ET\nfrom mathutils import Vector\nfrom mathutils.bvhtree import BVHTree\nEXPECTED=os.path.normcase(os.path.normpath(r'C:\\mcp-blender\\salvador_lacerda_do_zero.blend'))\nif os.path.normcase(os.path.normpath(bpy.data.filepath))!=EXPECTED: raise RuntimeError(f'ABORTADO arquivo errado {bpy.data.filepath!r}')\norigin=bpy.data.objects.get('WORLD | ORIGEM ELEVADOR LACERDA'); terrain=bpy.data.objects.get('WORLD | TERRENO MASTER | blockout'); slab=bpy.data.objects.get('V4 | CA | estacao laje 0'); hall=bpy.data.objects.get('V4 | CA | hall piso terreo marcador'); door=bpy.data.objects.get('V4 | CA | porta entrada placeholder')\nif not all((origin,terrain,slab,hall,door)): raise RuntimeError('anchors ausentes')\nM=ast.literal_eval(str(origin['utm_to_blender_matrix']))\ndef wgs_to_utm24s(lon,lat):\n a=6378137.0; f=1/298.257222101; e2=f*(2-f); ep2=e2/(1-e2); k0=.9996; lon0=math.radians(-39); p=math.radians(lat); l=math.radians(lon); N=a/math.sqrt(1-e2*math.sin(p)**2); T=math.tan(p)**2; C=ep2*math.cos(p)**2; A=math.cos(p)*(l-lon0); e4=e2*e2; e6=e4*e2; mm=a*((1-e2/4-3*e4/64-5*e6/256)*p-(3*e2/8+3*e4/32+45*e6/1024)*math.sin(2*p)+(15*e4/256+45*e6/1024)*math.sin(4*p)-(35*e6/3072)*math.sin(6*p)); E=500000+k0*N*(A+(1-T+C)*A**3/6+(5-18*T+T*T+72*C-58*ep2)*A**5/120); NN=k0*(mm+N*math.tan(p)*(A*A/2+(5-T+9*C+4*C*C)*A**4/24+(61-58*T+T*T+600*C-330*ep2)*A**6/720));\n if lat<0: NN+=1e7\n return E,NN\ndef to_bl(lon,lat):\n e,n=wgs_to_utm24s(lon,lat); return Vector((M[0][0]*e+M[0][1]*n+M[0][2],M[1][0]*e+M[1][1]*n+M[1][2]))\ndef fetch_way(oid):\n req=urllib.request.Request('https://api.openstreetmap.org/api/0.6/way/'+oid+'/full',headers={'User-Agent':'Salvador-Lacerda-QA/1.0'}); root=ET.fromstring(urllib.request.urlopen(req,timeout=15).read()); nd={n.attrib['id']:(float(n.attrib['lat']),float(n.attrib['lon'])) for n in root.findall('node')}; w=next(x for x in root.findall('way') if x.attrib.get('id')==oid); refs=[x.attrib['ref'] for x in w.findall('nd')]; pts=[to_bl(nd[r][1],nd[r][0]) for r in refs if r in nd]; return refs,pts,{t.attrib['k']:t.attrib['v'] for t in w.findall('tag')}\ntv=[terrain.matrix_world@v.co for v in terrain.data.vertices]; tp=[tuple(p.vertices) for p in terrain.data.polygons]; bvh=BVHTree.FromPolygons(tv,tp,all_triangles=False)\ndef tz(p):\n h=bvh.ray_cast(Vector((p.x,p.y,250)),Vector((0,0,-1)),600); return None if h[0] is None else float(h[0].z)\nrefs_c,cpts,ctags=fetch_way('1455480196'); refs_f,fpts,ftags=fetch_way('1429934820')\n# local frame\nc=slab.matrix_world.translation.xy.copy(); ang=slab.rotation_euler.z; AX=Vector((math.cos(ang),math.sin(ang))); AY=Vector((-math.sin(ang),math.cos(ang)));\ndef rows(refs,pts):\n out=[]\n for rid,p in zip(refs,pts):\n  r=p-c; z=tz(p); out.append({'id':rid,'xy':[round(p.x,4),round(p.y,4)],'local_xy':[round(r.dot(AX),4),round(r.dot(AY),4)],'terrain_z':None if z is None else round(z,4),'terrain_minus_hall_z':None if z is None else round(z-hall.matrix_world.translation.z,4)})\n return out\ncr=rows(refs_c,cpts); fr=rows(refs_f,fpts)\ndp=door.matrix_world.translation.xy.copy(); exitp=cpts[0] if (cpts[0]-fpts[0]).length<.05 else (cpts[-1] if (cpts[-1]-fpts[0]).length<.05 else fpts[0]); dist=(dp-exitp).length\n# sample interpolated first footway segment every meter\ntran=[]\nif len(fpts)>=2:\n a=fpts[0]; b=fpts[1]; L=(b-a).length; steps=max(1,int(math.ceil(L)))\n for i in range(steps+1):\n  d=min(float(i),L); p=a+(b-a).normalized()*d; z=tz(p); tran.append({'d_m':round(d,3),'xy':[round(p.x,4),round(p.y,4)],'terrain_z':None if z is None else round(z,4)})\nres={'corridor':{'id':'1455480196','tags':ctags,'nodes':cr},'footway':{'id':'1429934820','tags':ftags,'nodes':fr,'first_segment_terrain_transect':tran},'architecture':{'hall_z':round(hall.matrix_world.translation.z,4),'door_sill_z':round(min((door.matrix_world@Vector(v)).z for v in door.bound_box),4),'door_xy':[round(dp.x,4),round(dp.y,4)],'door_to_osm_path_exit_m':round(dist,4)},'interpretation_checks':{'shared_node':refs_f[0] if refs_f else None,'corridor_connects_bridge_to_footway':bool(refs_f and refs_f[0] in refs_c)}}\nprint('UPPER_PASSENGER_PATH_TERRAIN_AUDIT_BEGIN'); print(json.dumps(res,ensure_ascii=False,separators=(',',':'))); print('UPPER_PASSENGER_PATH_TERRAIN_AUDIT_END')"
  },
  "enabled": true
}
