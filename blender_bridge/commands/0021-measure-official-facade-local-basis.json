{
  "id": "0021-measure-official-facade-local-basis",
  "type": "execute_code",
  "params": {
    "code": "import bpy, math\nfrom mathutils import Vector\n\nbase=bpy.data.objects.get('OFICIAL | Entrada superior | lintel')\nassert base, 'lintel ausente'\ntheta=base.rotation_euler.z\nex=Vector((math.cos(theta), math.sin(theta), 0))\ney=Vector((-math.sin(theta), math.cos(theta), 0))\norigin=Vector((base.location.x, base.location.y, 0))\nprint(f'THETA={theta:.9f}|EX={tuple(round(v,6) for v in ex)}|EY={tuple(round(v,6) for v in ey)}|ORIGIN={tuple(round(v,4) for v in origin)}')\nfor name in ['OFICIAL | Entrada superior | fachada esquerda','OFICIAL | Entrada superior | lintel','OFICIAL | Entrada superior | fachada direita','OFICIAL | Entrada superior | cobertura']:\n    o=bpy.data.objects[name]\n    pts=[o.matrix_world @ Vector(c) for c in o.bound_box]\n    us=[(p-origin).dot(ex) for p in pts]; vs=[(p-origin).dot(ey) for p in pts]; zs=[p.z for p in pts]\n    print(f'{name}|U=({min(us):.4f},{max(us):.4f})|V=({min(vs):.4f},{max(vs):.4f})|Z=({min(zs):.4f},{max(zs):.4f})')\n"
  },
  "enabled": true
}
